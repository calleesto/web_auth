services:
  backend:
    image: backend
    ports:
      - 9002:8080
    build:
      context: .
      dockerfile: Backend/Dockerfile
  frontend:
    image: frontend
    ports:
      - 9003:80
    build:
      context: .
      dockerfile: Frontend/Dockerfile